---
import Layout from "@/components/Layout.astro";
import Page from "@/components/Page.astro";
import { getLinks } from "@teamgalena/shared/database";

const page = await getLinks({ size: 20 });
---

<Layout>
  <Page page={page} />
</Layout>
